# ssh

## ssh-keygen

```shell
ssh-keygen -t ed25519 -C "ansible" -f ./deploy/ansible/files/ssh_key/id-cat.ansible
ssh-keygen -t ed25519 -C "catsearch" -f ./deploy/ansible/files/ssh_key/id-cat.catsearch
```

